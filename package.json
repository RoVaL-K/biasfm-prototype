{
  "name": "biasfm-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "Statischer Prototyp: Landingpage + Konzeptseite für bias.fm",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx --yes serve ."
  },
  "dependencies": {
    "marked": "^16.0.0",
    "playwright": "^1.62.1"
  }
}
